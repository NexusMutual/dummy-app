name: Build

on:
  workflow_call:
    inputs:
      ref:
        type: string
        required: true
      image:
        type: string
        required: true
      dockerfile:
        type: string
        default: Dockerfile
      node-version:
        type: string
        default: 20
    secrets:
      github-token:
        required: true
      docker-username:
        required: true
      docker-password:
        required: true
    outputs:
      image-tag:
        value: ${{ jobs.build.outputs.image-tag }}

jobs:
  build:
    runs-on: ubuntu-22.04
    outputs:
      image-tag: ${{ steps.vars.outputs.TAG }}
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.github-token }}
          ref: ${{ inputs.ref }}

      - uses: actions/setup-node@v3
        with:
          node-version: ${{ inputs.node-version }}
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Get version and revision
        id: vars
        run: |
          VERSION=$(node -p 'require("./package.json").version')
          SHA_SHORT=$(git rev-parse --short HEAD)
          echo "TAG=${VERSION}-${SHA_SHORT}" >> $GITHUB_OUTPUT
          echo "REGISTRY=ghcr.io/${{ github.repository_owner }}" >> $GITHUB_OUTPUT

      - name: Build image
        env:
          IMAGE: ${{ inputs.image }}
          TAG: ${{ steps.vars.outputs.TAG }}
          REGISTRY: ${{ steps.vars.outputs.REGISTRY }}
        run: docker build -t "${REGISTRY}/${IMAGE}:${TAG}" -f ${{ inputs.dockerfile }} .

      - name: Push image
        env:
          IMAGE: ${{ inputs.image }}
          TAG: ${{ steps.vars.outputs.TAG }}
          REGISTRY: ${{ steps.vars.outputs.REGISTRY }}
        run: |
          echo ${{ secrets.DOCKER_PASSWORD }} | docker login ghcr.io -u ${{ secrets.DOCKER_USERNAME }}
          docker push "${REGISTRY}/${IMAGE}:${TAG}"

      - name: Print tag
        run: echo "Pushed ${{ inputs.image }}:${{ steps.vars.outputs.TAG }}"
